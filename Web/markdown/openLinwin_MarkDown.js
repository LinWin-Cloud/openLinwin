/**
 * Auther: LinwinCloud
 * https://github.com/LinWin-Cloud/
 */

window.onload = function() {
    var str = document.getElementById('markdown').innerText;
    
    if (str.indexOf("** ") != -1) {
    }
}
